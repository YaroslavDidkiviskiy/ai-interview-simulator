server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /auth/ {
        proxy_pass http://app:8000;
    }

    location /api/ {
        proxy_pass http://app:8000;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}